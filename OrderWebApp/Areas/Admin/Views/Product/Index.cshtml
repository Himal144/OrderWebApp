@model List<Product>

<partial name="_Notification"></partial>
<div class="container ">
    <div class="row pt-4 pb-2">
        <div class="col-6">
            <h2>Product List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">  <i class="bi bi-plus-circle"></i>          Add new Product</a>
        </div>
    </div>
</div>
<table id="tblData" class="table table-bordered table-striped">
    <thead>
    <tr>
        <th class="text-center">Product Title</th>
        <th class="text-center"> Description</th>
        <th class="text-center">ISBN</th>
        <th class="text-center">Author</th>
        <th class="text-center">Category</th>
        <th class="text-center">List Price</th>
        <th class="text-center">Price(1-50)</th>
        <th class="text-center">Price(50-100)</th>
        <th class="text-center">Price(100+)</th>
         <th class="text-center"> Manage </th>

        
    </tr>
    </thead>
  @*   <tbody>
        @foreach (var obj in Model)
        {    
            <tr>
                <td class="text-center">@obj.Title</td>
                <td class="text-center">@obj.Description</td>
                 <td class="text-center">@obj.ISBN</td>
                     <td class="text-center">@obj.Author</td>
                         <td class="text-center">@obj.CategoryId</td>
                         <td class="text-center">@obj.ListPrice</td>
                             <td class="text-center">@obj.Price</td>
                                 <td class="text-center">@obj.Price50</td>
                                     <td class="text-center">@obj.Price100</td>
                                    
                <td class="text-center">
                        <div class="btn-group w-75 text-center">
                                    <a asp-controller="Product" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-secondary mx-1"><i class="bi bi-pencil-square"></i> Edit </a>
                                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-1"><i class="bi bi-trash3"></i>Delete </a>
                        </div>
                </td>
            </tr>
        } 
    </tbody> *@
</table>

                        @section scripts{
                            <script src="~/js/product/product.js"></script>
                        }